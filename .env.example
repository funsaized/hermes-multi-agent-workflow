# Copy to .env (per Hermes profile, as needed) and fill in. NEVER commit .env.
# These are consumed by your Hermes profiles, not by the engine directly.

# --- Fresh Discord gateway setup only ---
# Existing root/default gateways reuse their configured token and authorization;
# do not duplicate those values in this repository or in scout profiles.
DISCORD_BOT_TOKEN=
# One possible inbound authorization method; pairing/roles/gateway allowlists may
# already cover an existing installation. This is a user id, not a channel id.
DISCORD_ALLOWED_USERS=
DISCORD_HOME_CHANNEL=1484142557704491119  # Gaymerz / #briefs

# --- Web search for scouts (scout profiles) ---
# Provide whichever backend your Hermes web toolset uses.
TAVILY_API_KEY=
# SERPER_API_KEY=
# BRAVE_API_KEY=

# --- Engine overrides (optional; defaults derive from triage.yaml) ---
# TRIAGE_CONFIG=triage.yaml
# TRIAGE_VAULT_DIR=./work/vault/items
# HERMES_KANBAN_DB=               # normally injected by the dispatcher per task
